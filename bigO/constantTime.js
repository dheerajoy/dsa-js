// Constant time O(1)

// The Run time doesn't change regardless of input size

function getFirstElement(arr) {
  return arr[0];
}

// This function will always take same amount of time, no matter how large the input is.

// console.log(sum(10));

// console.log(this);
